1. [x] Créer le système d'animation, pour une entité, le entity2d peut l'override, on doit pouvoir faire anim.start() et passer en paramètre soit une liste de liens vers un asset, soit une sprite sheet avec des dimensions de précisé
2. [x] Charger les animations depuis `player.json` dans `Entity`, les enregistrer dans le constructeur de `Player` via `animationPath` et démarrer `Jump_Left` depuis `Jump.cpp`.
3. [x] Créer un système box collider à part du sprite, par défaut quand pour l'entité useCollision=true, le box collider est de la taille du sprite, mais on peut override la taille et la position du box collider avec `setCollisionBox`.
4. [x] Là, avec `player_automatic.json` j'utilise une spritesheet qui est de gauche à droite et chaque ligne est une animation, mais rien ne dit que ce sera toujours le cas, une animation pourrait être tenu sur plusieurs lignes, ça pourrait être vertical de haut en bas et ainsi de suite. Le système fonctionne, mais n'est pas universel
5. Créer le système de gravité et permettre de définir une entité comme utilisant la gravité à la création de l'entité, de ce fait le fonctionnement du jump du player changerait.
6. [x] Ajouter aux entités une position Z (0 par défaut), configurable dans le constructeur, avec `setPosition(x, y, z)` ou `setZIndex(z)`.
7. Refactorer les objets du moteur afin d’améliorer la séparation entre les fonctionnalités génériques et celles spécifiques à la 2D, en prévision d’une éventuelle prise en charge de la 3D (Ne pas faire de 3D, ici on prépare simplement le terrain). Les objets actuellement spécifiques à la 2D devraient être renommés avec le suffixe 2D, tandis qu’un objet générique pourrait être introduit pour servir de base aux futures implémentations 2D et 3D. Par exemple, créer un objet Animation générique et renommer l’implémentation actuelle en Animation2D, comme pour Entity et Entity2D. Identifier également les autres objets concernés afin de conserver une architecture cohérente et faciliter l’ajout éventuel de fonctionnalités 3D.
8. Ajouter un événement OnClick aux Entity2D, sur le même principe que l’événement OnCollision, afin de permettre aux entités de réagir aux interactions de la souris. Cette fonctionnalité permettrait d’élargir les possibilités du moteur au-delà de la création de jeux de plateforme, notamment pour faciliter le développement de jeux de gestion ou d’autres jeux reposant davantage sur l’interaction avec les éléments à l’écran.
